div(class="bg-white shadow rounded-lg")
    img(class="object-cover h-72 w-full" src=`/uploads/${prop.image}` alt=`Imagen casa: ${prop.name}`)
    div(class="p-5 space-y-3")
        h3(class="text-2xl font-bold")= prop.title
        p(class="text-sm text-gray-600") Habitaciones: 
            span(class="text-gray-800 font-bold")= prop.bedrooms
        p(class="text-sm text-gray-600") Baños: 
            span(class="text-gray-800 font-bold")= prop.bathrooms
        p(class="text-sm text-gray-600") Precio: 
            span(class="text-gray-800 font-bold")= prop.price.name
        a(href=`/property/${prop.id}` class="bg-indigo-600 w-full text-center block font-bold text-white p-2 uppercase rounded-md") Ver propiedad